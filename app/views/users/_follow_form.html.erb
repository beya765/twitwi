<!-- フォロー/フォロー解除フォームのパーシャル -->
<!-- 自分以外のユーザー画面で表示 -->
<% unless current_user?(@user) %>
  <div id="follow_form">
  <% if current_user.following?(@user) %>
    <%= render 'unfollow' %>
  <% else %>
    <%= render 'follow' %>
  <% end %>
  </div>
<% end %>